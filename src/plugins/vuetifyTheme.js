export default {
  theme: {
    // Default Theme
    dark: true,
    // Define Themes
    themes: {
      light: {
        primary: "#FFB300"
      },
      dark: {
        primary: "#FFB300"
      }
    }
  },
  icons: {
    iconfont: "mdiSvg" // 'mdi' || 'mdiSvg' || 'md' || 'fa' || 'fa4' || 'faSvg'
  }
};
